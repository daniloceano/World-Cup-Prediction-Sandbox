# WCPS — World Cup Prediction Sandbox
# Minimal, lightweight dependencies (Streamlit ships Altair for charts).
streamlit>=1.30
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
altair>=5.0
pytest>=7.0   # tests only
